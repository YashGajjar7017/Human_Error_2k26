name: synapseml
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - jupyter
  - notebook
  - pip:
    - synapseML
    - pyspark
    - azure-storage-blob
    - azure-identity
